# CLAUDE.md — Backend (Flask + Firestore + OpenAI)

Local conventions for the backend. The root `CLAUDE.md` carries product context, the Firestore schema, environment variables, and repo-wide conventions — both load together.

**Code split:** the Flask entrypoint and core modules live at the **repo root** (`main.py`, `database.py`, `scoring.py`), while blueprints, services, the DI container, Postgres-migration models, and tests live here under `backend/`.

## Commands

```bash
python3 -m venv .venv && . .venv/bin/activate
pip install -r requirements.txt
PORT=5001 FLASK_ENV=development python main.py   # localhost:5001 (matches Vite proxy)
```

Run from the repo root — that is where `main.py` is. `main.py` fast-fails on missing required env vars in production and warns in dev; see `_validate_required_env` for the required/feature-gated lists (and the root `CLAUDE.md` Environment Variables section).

- Backend tests: `make test-backend`, or one file: `python3 -m unittest backend.tests.test_curriculum_admin_routes -v`
- Firestore-emulator integration: `make test-emulator`

## Stack

Flask 3.1, Firebase Admin SDK, Firestore, OpenAI Realtime + Chat APIs, flask-sock for websockets, PyLTI1p3 for LTI 1.3.

## Dependency injection pattern

`main.py` builds a `RouteDeps` (`backend/route_deps.py`) that carries `db`, `firebase_auth`, session helpers, OpenAI client, prompt builders, school-context resolvers, and allowed-locale sets. Every blueprint is registered via a `create_*_blueprint(deps)` factory. **New routes must follow this pattern** — never import `main` or module-level singletons directly.

## Blueprints (`backend/routes/`)

`auth`, `chat`, `assessment`, `pronunciation`, `games`, `schools`, `guardian`, `teacher`, `curriculum_admin`, `admin`, `integrations` (Canvas), `canvas_practice`, `school_requests`, `lti`. `test_harness` is registered only in development/testing and exposes `/api/test/*` for E2E.

## Services (`backend/services/`)

Domain logic that blueprints compose.
- `assignment_resolver.py` — assembles assignment-aware system prompts from assignment-owned fields + student profile + compliance policy + modality policy
- `practice_analytics.py` — session summary building, learning event rollup, class/assignment/student aggregation
- `membership_context.py` — request-level resolution of active org + role + classes
- `compliance.py`, `disclosure_logging.py`, `deletion_requests.py`, `guardian_packets.py` — the compliance surface
- `canvas/` — Canvas LMS client, AES-256-GCM PAT encryption, roster sync, practice generator
- `lti/` — LTI 1.3 identity, config, grade passback, JWKS keys
- `pedagogy/` — the **Pedagogy Engine** (S1 thin spine + S2 closed-loop recycling + S3.1 post-task coach review + S3.2 live between-turn coach chips + S3.3 promote-back + correction-light, behind flag + S3.4 Ask mode, behind flag + S4.1 affect-aware tutoring, behind flag + S4.2 teacher debrief, behind flag + S5 Director drift re-steer, behind flag). `policies.py` (teacher-policy normalizers, relocated from the resolver), `routing.py` (target-type-aware feedback routing — the behaviour win), `plan.py` (`compile_prompt_plan` → `PromptPlan` + `serialize_plan_preview`), `coverage.py` (stdlib-only S2 reader: `compute_coverage_state(...) → CoverageState` — count-only tiering of expression+vocabulary targets, no DB/OpenAI imports), `coach_review.py` (stdlib-only S3.1+S3.2 pure module: `CoachReview` schema + `build_coach_review_prompt` / `parse_coach_review` / `serialize_coach_review`, `feedback_mode`-keyed `work_on` depth caps; **S3.2 additions:** `build_coach_chip_prompt` / `parse_coach_chip` / `serialize_coach_chip` — no DB/OpenAI imports), `promote_back.py` (stdlib-only S3.3 pure decision module: `PromoteDecision` dataclass + `decide_promote_back(promote_state, chip, feedback_policy, turn_index) → (PromoteDecision, dict)` + `build_promote_prompt(chip, surface) → str`; recurrence counter + mode-modulated thresholds + 3 guards: cooldown/per-session-cap/reset-on-promote — no DB/OpenAI imports), `ask.py` (stdlib-only S3.4 pure module: `build_ask_prompt(question, recent_turns, targets, feedback_policy, scaffold_policy, surface, ui_language) → list[message]` anti-answer-dump contract + `parse_ask_answer(raw) → AskAnswer{answer, kind}` + `serialize_ask_answer(answer) → dict` — no DB/OpenAI imports), `affect.py` (stdlib-only S4.1 pure module: `compute_affect_state(session_signals) → AffectState` + `affect_stance_lines(affect, *, correction_light=False) → list[str]` + `serialize_affect_state(affect) → dict`; constants AFFECT_WINDOW_SESSIONS=3, MIN_SESSIONS_FOR_AFFECT=2, REPAIR_DENSITY_HIGH=0.6, ABANDONMENT_STRAIN_MIN=2, TURN_TREND_FALL_RATIO=0.7; readiness values `"settled"` | `"neutral"` | `"strained"` — no DB/OpenAI imports), `debrief.py` (stdlib-only S4.2 pure presenter: `build_session_debrief(session_record: Any) -> dict` — total/no-raise; projects `analysis_state` + `session_summary` into the L7 debrief shape; `MAX_SUGGESTIONS=4`; static honesty `caveats` always present; **L7 cross-slice (2026-06-24):** also shapes two intervention sections — `directorReSteers` = `{count, items:[{turnIndex,kind,target,reason}]}` (`_director_resteers` from `analysis_state['resteers']`, S5 Director) and `promotions` = `{count, items:[{turnIndex,reason,target}]}` (`_promotions` from `analysis_state['promotions']`, S3.3 promote-backs — `reason` "repeat"|"hard_target", `target` = signature with leading `focus_grammar:` stripped; **was a raw `_l` pass-through, now shaped**). Both omit internal `prompt`/`surface`/`generated_at` (teacher sees the intervention's meaning, not the coach-note) — no DB/OpenAI imports), `assignment_debrief.py` (stdlib-only S4.2b pure aggregator: `build_assignment_debrief(session_records) -> dict` — pools the per-session `build_session_debrief` coaching signals [uptake/promotions-by-target/re-steers-by-kind/help/affect-distribution/coach-review-count] + a participation denominator into a class-level coaching roll-up; deliberately NOT content coverage [analytics owns that]; total/no-raise; reuses `debrief.py` helpers `_d`/`_i`/`_l`/`_ASK_KINDS`/`_CAVEATS`; no DB/OpenAI imports), `drift.py` (stdlib-only S5 pure module: **two drift signals** `detect_target_neglect(recent_tutor_turns, concrete_targets, *, window) -> DriftVerdict` (kind `target_neglect`) + `detect_language_drift(latest_tutor_turn, learning_locale) -> DriftVerdict` (kind `language_drift` — non-Latin ko/ru/he target-script ratio < `TARGET_SCRIPT_MIN_RATIO`=0.5, Latin es/fr/tl ≥ `ENGLISH_MARKER_MIN_HITS`=3 distinct English function words; `LANGUAGE_DRIFT_MIN_CHARS`=12 guard); `decide_resteer(director_state, verdict, turn_index) -> (ResteerDecision, dict)` / `build_resteer_prompt(verdict, *, surface) -> str` (kind-branched copy) / `serialize_resteer(decision, *, turn_index, surface, prompt, generated_at) -> dict`; constants `DRIFT_WINDOW=3` / `DIRECTOR_COOLDOWN_TURNS=4` / `DIRECTOR_MAX_RESTEERS=3` — no DB/OpenAI imports), `render/assignment_prompt.py` (`render_assignment_prompt(plan, surface)` + the recycling section + `correction_light` kwarg for S3.3), `integration.py` (flag-gated dispatch + `recycling_enabled()` / `coach_review_enabled()` / `coach_chips_enabled()` / `promote_back_enabled()` / `ask_mode_enabled()` / `affect_enabled()` / `debrief_enabled()` / `director_enabled()` / `teacher_preview_enabled()` / `debrief_rollup_enabled()` env helpers; `promote_back_enabled()` reads ONLY `PEDAGOGY_ENGINE_PROMOTE_BACK` — the two-flag safety invariant (correction-light requires BOTH `PROMOTE_BACK=1` AND `COACH_CHIPS=1`) is applied at the render seam `resolve_assignment_system_prompt`, which ANDs `promote_back_enabled()` and `coach_chips_enabled()`). **Import boundary (enforced + tested):** `plan.py`/`routing.py`/`coverage.py`/`coach_review.py`/`promote_back.py`/`ask.py`/`affect.py`/`debrief.py`/`assignment_debrief.py`/`drift.py` import no OpenAI/Canvas/resolver — verified by `test_pedagogy_engine_s1.ImportBoundaryTestCase`. The S4.2 **pure** presenter is called directly by the route handler `GET /api/teacher/practice-sessions/<session_id>/debrief` (flag gate first; session→assignment→class→teacher auth via `_require_assignment_teacher_access`; fail-soft on assembly exception; `debriefEnabled: debrief_enabled()` also on the three teacher analytics payloads). The S4.1 **impure** reader is `practice_analytics.compute_assignment_affect_state(db, bootstrap, uid, assignment_id, *, current_session_id=None) → AffectState | None` (mirrors `compute_assignment_coverage_state`: `affect_enabled()` gate OUTSIDE the try, first session = neutral, fail-open → None; built by `_affect_session_signals` which reads avg_words from the raw session_summary); `analysis_state['affect_state']` snapshot at session create. The S3.1 **impure** orchestrator lives outside the package at `backend/services/coach_review_service.py` (`generate_coach_review(deps, bootstrap, uid, session_id) → dict | None`: transcript fetch + OpenAI call + fail-open snapshot into `analysis_state['coach_review']`), behind `GET /api/practice-sessions/<id>/coach-review` (generate-on-read, cached). The S3.2 **impure** orchestrator lives at `backend/services/coach_chip_service.py` (`generate_coach_chip(deps, bootstrap, uid, session_id, turn_index) → dict | None`: heuristic-gated per-turn chip generation + fail-open append into `analysis_state['coach_chips']` via `update_practice_session_analysis_state`; **S3.3 addition:** after chip generation, calls `decide_promote_back` and merges `promote`/`promote_prompt`/`promote_reason` into the chip dict), behind `POST /api/practice-sessions/<id>/coach-chip`. The S3.4 **impure** orchestrator lives at `backend/services/ask_service.py` (`answer_ask(deps, bootstrap, uid, session_id, question, turn_index=None) → dict | None`: one LLM call + fail-open append into `analysis_state['ask_log']` via `update_practice_session_analysis_state`; `ASK_MODEL = "gpt-5.4-mini-2026-03-17"`, `reasoning_effort="high"`), behind `POST /api/practice-sessions/<id>/ask`. The S5 **impure** orchestrator lives at `backend/services/director_service.py` (`assess_drift(deps, bootstrap, uid, session_id, turn_index) → dict | None`: no LLM call, pure-heuristic; reads session + chat transcript; runs `detect_language_drift` (precedence) → `detect_target_neglect` (fallback) → `decide_resteer`; re-read-before-write; dedup by `turn_index`; fail-open; persists `director_state` + `resteers[]` into `analysis_state`; returns re-steer payload or `None`), behind `POST /api/practice-sessions/<id>/coach-chip` (rides the same round-trip as S3.2/S3.3; route returns `resteer` alongside `coachChip`). **Flag state:** S1 assignment render is now **unconditional** (the `PEDAGOGY_ENGINE_ASSIGNMENT_RENDER` flag + legacy `build_assignment_system_prompt` were retired 2026-06-23); the S2 recycling loop is gated by `PEDAGOGY_ENGINE_RECYCLING` (LIVE, cloudbuild default `'1'`, route compute fail-open); the S3.1 coach review is gated by `PEDAGOGY_ENGINE_COACH_REVIEW` (LIVE, cloudbuild default `'1'`, fail-open to `coachReview: null`); the S3.2 coach chips are gated by `PEDAGOGY_ENGINE_COACH_CHIPS` (**LIVE, cloudbuild default `'1'`, cut over 2026-06-24**, rev `lingual-app-00078-wrc`; gate runs before any read/LLM, fail-open; rollback via `--update-env-vars PEDAGOGY_ENGINE_COACH_CHIPS=0`); the S3.3 promote-back decision + correction-light stance is gated by `PEDAGOGY_ENGINE_PROMOTE_BACK` (**LIVE — CUT OVER 2026-06-25, rev lingual-app-00084-cbx, cloudbuild default `'1'`**; two-flag safety invariant satisfied (COACH_CHIPS=1 live); burn-in caveat: rides the feedback heuristic → under-correction risk until catalogs proven on real traffic; rollback via `--update-env-vars PEDAGOGY_ENGINE_PROMOTE_BACK=0`); the S3.4 Ask mode is gated by `PEDAGOGY_ENGINE_ASK_MODE` (**LIVE — CUT OVER 2026-06-25, rev lingual-app-00084-cbx, cloudbuild default `'1'`**; prod runtime-verified with the test student — scaffolded assignment returned an anti-answer-dump hint [`"Quisiera un ___, por favor"` fill-in, not the full answer] + HELP≠EVIDENCE caveat; scaffold-free assignment returns null per the `if not targets` guard [by design, not a bug]; `/ask` returns null + Ask tab hidden when off; rollback via `--update-env-vars PEDAGOGY_ENGINE_ASK_MODE=0`); the S4.1 affect-aware tutoring is gated by `PEDAGOGY_ENGINE_AFFECT` (**LIVE — CUT OVER 2026-06-25, rev lingual-app-00084-cbx, cloudbuild default `'1'`**; gentler stance when prior-session signals show strain, prompt byte-identical when neutral/settled [first session = neutral]; voice surface NOT headlessly verifiable → burn-in only; rollback via `--update-env-vars PEDAGOGY_ENGINE_AFFECT=0`); the S4.2 teacher debrief is gated by `PEDAGOGY_ENGINE_DEBRIEF` (**LIVE — CUT OVER 2026-06-24, rev lingual-app-00080-2lh, cloudbuild default `'1'`**; deployed inert on rev 00079 then flipped + prod runtime-verified with the test teacher account [endpoint 200, all sections render, intervention cards self-hide at count 0]; debrief endpoint returns `{success: false}`/`debriefEnabled: false` when off; rollback via `--update-env-vars PEDAGOGY_ENGINE_DEBRIEF=0`); the S5 Director drift re-steer (v2: detects **target-neglect AND language-drift**, language-drift taking precedence) is gated by `PEDAGOGY_ENGINE_DIRECTOR` (**BUILT, cloudbuild default `'0'`, NOT cut over** — flag absent/off in live prod; `assess_drift` returns `None` immediately when off, route returns `resteer: null`, behavior byte-identical to today; cutover gated on the S5-gate eval plateau verdict; rollback via `--update-env-vars PEDAGOGY_ENGINE_DIRECTOR=0`); the **L8 Teacher Preview** is gated by `PEDAGOGY_ENGINE_TEACHER_PREVIEW` (**LIVE — CUT OVER 2026-06-24, rev lingual-app-00080-2lh, cloudbuild default `'1'`**; prod runtime-verified [the builder "Toggle AI engine preview" panel rendered both the raw-mode notice and the full engine plan table with target-type-aware routes]) — a read-only teacher endpoint `GET /api/teacher/assignments/<id>/plan-preview` returns `serialize_plan_preview(compile_prompt_plan(bootstrap))` (the BASE plan — no coverage/affect) for the assignment's teacher (`_require_assignment_teacher_access` → `resolve_assignment_bootstrap_for_user` teacher path), compile-on-read, fail-soft (`planPreview: null` never 500), surfaced as a flag-gated "How the AI will run this" panel in the assignment builder; flag off ⇒ `teacherPreviewEnabled: false`, panel hidden, builder byte-equivalent; rollback via `--update-env-vars PEDAGOGY_ENGINE_TEACHER_PREVIEW=0`. The **S4.2b assignment-level coaching debrief roll-up** is gated by `PEDAGOGY_ENGINE_DEBRIEF_ROLLUP` (**LIVE — CUT OVER 2026-06-25, rev lingual-app-00083-lf4, cloudbuild default `'1'`** — gate = `debrief_enabled() AND debrief_rollup_enabled()`; read-only teacher endpoint `GET /api/teacher/assignments/<id>/debrief` aggregates the engine coaching-process signals across an assignment's sessions via the pure `build_assignment_debrief`; reached by a gated "View coaching debrief" link on the assignment-analytics page [which now also returns `debriefRollupEnabled`]; fail-soft never-500; off ⇒ `{success: false}` + no link, byte-equivalent UI; deployed inert rev 00081, flipped rev 00082, prod runtime-verified — which **caught a studentCount=0 bug**: read PG col `student_firebase_uid` but the serialized practice_session dict uses `student_uid` (`analytics_reads._serialize_session` renames it; the class-analytics route reads the same key) → TDD-fixed + re-verified `"1 student(s)"` on rev 00083; rollback via `--update-env-vars PEDAGOGY_ENGINE_DEBRIEF_ROLLUP=0`). Feedback catalog covers **en/fr/es/ko/ru/he/tl natively** (locale→catalog map `_FEEDBACK_LOCALE_CATALOGS` in `practice_analytics.py`). Non-Latin matching was **repaired 2026-06-24**: `_normalize_search_text(content, locale)` is script-aware (Latin keys ascii-strip for accent-insensitive matching; `ko`/`ru`/`he` preserve script via NFKC+casefold). Previously the unconditional ascii-strip reduced Hangul/Cyrillic/Hebrew to empty *before* matching, silently disabling feedback detection, target-expression hit-counting (S2 coverage), and signal matching for those 3 locales. `_count_target_expression_hits(content, expressions, *, locale)` is now locale-threaded. Tagalog (`tl`) is Latin-script (uses the ascii path) and gained a native catalog. STILL deferred for non-Latin (the `locale_catalogs` map makes them addable): native student-error rules (`ERROR_RULES` is en/fr/es-only) and comm-function/discourse-move/context-tag catalogs. Feedback patterns are heuristic first-sets, refinable from real-session phrasing (same posture as the Spanish catalog). **S5-gate eval instrument** (built 2026-06-24, RAN 2026-06-24 → 3/3 NO PLATEAU → S5 Director deferred with evidence, `PEDAGOGY_ENGINE_DIRECTOR` stays `'0'`): pure verdict module `backend/tests/eval/adherence_drift.py` (CI-tested, zero cost) + opt-in harness `backend/tests/eval/test_static_composition_drift_eval.py` (simulated-student↔tutor loop + per-turn adherence judge; full LLM run gated by `RUN_PEDAGOGY_EVAL=1`); decides whether between-turns `session.update` re-steer (S5) is warranted — see `PEDAGOGY_ENGINE_S5.md`. See `docs/school-integration/Pedagogy Engineering/`.
- `assignment_workspace.py` — teacher-side assignment authoring helpers

## Cloud SQL (PostgreSQL) migration layer (`backend/db/`)

Migration of the school domain to Cloud SQL Postgres as system-of-record (ADR-0001). Holds SQLAlchemy `models/`, `repository/`, Alembic `migrations/`, the dual-write paths (`dual_write.py`, `dual_write_school_chain.py`, `dual_write_analytics.py`), and the `ReadRouter` (`read_router.py`) on `deps.db`.

**Current state (2026-06-03 — read cutover COMPLETE):**
- **Reads:** all PG-authoritative (org/class/enrollment/assignment/practice_sessions/learning_events), fail-open to Firestore via the 3-state `READ_PG_*` flags (`''`/`'0'`=Firestore, `shadow`=compare-only, `'1'`=PG-authoritative). The one exception is **memberships** (`READ_PG_MEMBERSHIPS=shadow`): the role-guard `resolve_user_school_context` reads the in-module Firestore primitive, so memberships stay **Firestore-read-authoritative by design** (D4) — see LIMITATIONS.
- **Writes — analytics family RETIRED:** `practice_sessions` + `learning_events` are **Postgres-sole** (`WRITE_FIRESTORE_ANALYTICS=0`); the `primary_*` paths are **fail-CLOSED** (a PG failure surfaces as a 500 the SPA retries, not a silent drop).
- **Writes — relational/assignment family STILL DUAL-WRITE (intended steady state, NOT a TODO):** org/class/enrollment/assignment continue to dual-write to Firestore *and* PG (`DUAL_WRITE_SCHOOL_CHAIN`/`DUAL_WRITE_ENROLLMENTS`/`DUAL_WRITE_ASSIGNMENTS=1`), fail-open. **Do not "finish the migration" by retiring these Firestore writes.** Keeping them is deliberate: the Firestore mirror is the instant **read-rollback bridge** (flip `READ_PG_*=shadow` and reads fall back to a live Firestore copy). Retiring relational writes also first requires moving the role-guard off Firestore memberships (security-critical). Revisit only post-beta on a real trigger (long PG confidence at scale, or doing the role-guard work anyway), not as cleanup.

Never move a read path's flag, or touch a dual-write seam, without checking the live flag state first (`gcloud run services describe lingual-app`). This is the sanctioned exception to the root "Firestore for beta" convention.

## Assignment content lives on the assignment document

The resolver reads `instructions`, `generated_scenario`, `objectives`, `target_expressions`, `focus_grammar`, `teacher_notes`, `task_type`, `target_language_intensity`, and (optionally) `canvas_module_item_ref` directly — there is no separate curriculum-overlay collection. `task_type: custom_prompt` is a scaffold-free mode that bypasses scenario generation and rubric-dependent analytics (see LIMITATIONS.md #14).

## Request flows

- **Auth:** Firebase ID token → `POST /api/auth/verify` verifies token, creates Flask session, returns memberships + active org context. `MembershipContext` on the frontend consumes this.
- **Realtime:** `POST /api/realtime/session` mints an ephemeral OpenAI Realtime credential → frontend connects via `useRealtimeChat`. Voice is compliance-gated and fails closed without consent.
- **SPA serving:** in production, Flask serves `static/react/` (built by the frontend Docker stage). Never hand-edit `static/react/`.

## Key files

- `main.py` (repo root) — Flask app, env validation, OpenAI client factory, prompt builders, blueprint registration
- `database.py` (repo root) — Firestore CRUD for all collections
- `scoring.py` (repo root) — assessment scoring + ACTFL description lookup
- `backend/route_deps.py` — DI container injected into every blueprint
- `backend/routes/curriculum_admin.py` — assignment CRUD, practice session creation, event reporting, analytics
- `backend/routes/teacher.py`, `schools.py`, `admin.py` — teacher + school-admin + Lingual-admin surfaces
- `backend/routes/integrations.py`, `canvas_practice.py` — Canvas LMS
- `backend/routes/lti.py` — LTI 1.3 launch, link-account, assignment picker, grade passback
- `backend/routes/guardian.py`, `school_requests.py` — compliance + school-request lifecycle
- `backend/services/assignment_resolver.py` — assignment-aware prompt assembly
- `backend/services/practice_analytics.py` — session summaries + analytics aggregation

## Conventions

- **Never route practice through a generic chat prompt** when an assignment context exists — always go through `assignment_resolver`.
- Implementation conventions (test framework, DI patterns, naming, Cloud Function `_impl`+wrapper split, outbox usage, Plan 1 contract surface): see `docs/superpowers/codebase-conventions.md`. Read it before writing plan code.
